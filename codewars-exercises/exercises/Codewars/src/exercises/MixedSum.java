package exercises;

import java.util.List;
import java.util.stream.Stream;

public class MixedSum {
	public int sum(List<?> mixed) {	
//		return Stream.of(mixed).map(item -> Integer.parseInt(item));
		return 1;
	}
}
